/**
 * @jsx React.DOM
 */

var React = require('react/addons');

var Tag = require('./tag');

var TagList = React.createClass({

    render: function() {
        return (
            <div />
        );
    }

});

module.exports = TagList;
